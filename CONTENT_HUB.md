# 성글벙글 콘텐츠 허브 — 운영 기준

매일 오전 9시(한국시간)에 새 원고를 만들어 **고정 링크 하나**를 갱신하는 운영 문서다.
새 링크를 만들지 않는다. 항상 같은 주소를 덮어쓴다.

---

## 1. 파일 구조

| 경로 | 역할 |
|---|---|
| `CONTENT_HUB.md` | 전체 운영 규칙 (이 문서) |
| `content-hub-site/index.html` | 콘텐츠 허브 화면 |
| `content-hub-site/content-data.js` | **오늘자** 콘텐츠 |
| `content-hub-site/format-rules.js` | 원고 서식 규칙 (화면 표시 + 검수 기준) |
| `content-hub-site/content-history.json` | 중복 확인용 **경량 이력** (주제·제목·키워드만) |
| `content-hub-site/archive/content-data-YYYY-MM-DD.js` | 과거 콘텐츠 보관 |
| `scripts/rotate-content.mjs` | 보관 → 이력 추가 자동화 |
| `scripts/validate-content.mjs` | 문법·분량·구성·중복 일괄 검수 |
| `.github/workflows/deploy-pages.yml` | 푸시 시 고정 링크 자동 배포 |

---

## 2. 매일 오전 9시 실행 순서

1. `content-history.json`을 읽는다. **과거 원고 본문은 절대 읽지 않는다.**
2. 완료/미완료 상태를 확인한다.
3. 기존 주제와 겹치지 않는 시의성 높은 주제를 선정한다.
4. 정부·공공기관·기업 **공식 자료를 기본 최대 3개**까지만 조사한다.
   정보가 충돌하거나 부족할 때만 추가 조사한다.
5. **MASTER FACT를 한 번만** 작성한다. 날짜·금액·대상·조건을 여기에 확정한다.
6. 네이버 3편, 티스토리 3편, 카드뉴스 6세트를 제작한다. MASTER FACT를 전 플랫폼에 재사용한다.
7. `node scripts/rotate-content.mjs` — 기존 원고를 날짜별 보관 파일로 복사한다.
8. `content-data.js`를 오늘자 콘텐츠로 교체한다.
9. `content-history.json`에 오늘 주제·제목·키워드를 추가한다. (7번 스크립트가 처리)
10. `node scripts/validate-content.mjs` — 문법·분량·구성·사실 일치를 **한 번에** 검사한다.
11. 실패한 항목만 수정하고 **한 번만** 재검사한다.
12. 검수 통과 시에만 커밋 + 푸시한다. 배포는 **한 번만** 실행한다.
13. 고정 링크를 **한 번만** 열어 오늘 날짜와 원고 수를 확인한다.
14. 저장·배포·링크 확인이 모두 성공했을 때만 성공으로 보고한다.

> 긴 중간 보고는 생략한다. 최종 결과만 보고한다.

---

## 3. 콘텐츠 제작 기준

### 네이버 블로그
- 하루 **3편**
- 편당 **3,200~3,500자** (본문 기준, 공백 포함)
- 원고별 이미지 기획 **8개**
- 지정 인사말 사용
- 😊 · 💙 · 😎 **각각 정확히 1회**
- 친근하고 자연스러운 정보형 문체

### 티스토리
- 하루 **3편**
- 편당 **2,501~3,000자**
- 원고별 이미지 기획 **8개**
- 네이버와 **다른** 제목·도입·구성
- Google 검색형 SEO 구조 (H2/H3, 메타 설명, FAQ)

### 카드뉴스
- 하루 **6세트**
- 세트당 **7장**
- 모바일에서 빠르게 읽히는 핵심 정보형 구성

### 공통
- 날짜·금액·대상·조건은 **MASTER FACT와 일치**해야 한다.
- 공식 발표와 예상 정보를 명확히 구분한다.
- 플랫폼 사이 **문장 복사 금지**.
- 기존 원고는 참고만 하고, 새 검색 의도와 구성으로 작성한다.
- 제목은 과거 이력과 중복되지 않아야 한다.

---

## 4. 사용량 절약 규칙

이 규칙들이 사용량 급소진의 직접 원인을 제거한다.

| 항목 | 기준 |
|---|---|
| 과거 원고 확인 | 본문 전체 읽기 **금지**. `content-history.json`만 읽는다 (약 1,000자) |
| 자료 조사 | 기본 최대 3개. 충돌·부족 시에만 추가 |
| MASTER FACT | 하루 **1회** 작성 후 전 플랫폼 재사용 |
| 검수 | 문법·분량·구성 **한 번에** 실행. 실패 항목만 1회 재검사 |
| 배포 | **1회** |
| 링크 확인 | **1회** |
| 실행 방식 | 매일 **독립 실행** (이전 대화 누적 금지) |
| 중간 보고 | 생략. 최종 결과만 |

---

## 5. 고정 링크

배포 대상 링크는 `content-hub-site/site-config.json`의 `publicUrl`에 기록한다.
이 값은 **변경하지 않는다.** 매일 같은 주소를 갱신한다.

---

## 6. 검수 통과 조건

`scripts/validate-content.mjs`가 검사하는 항목:

- 자바스크립트 문법 오류 없음
- 네이버 3편 / 티스토리 3편 존재
- 카드뉴스 6세트 × 각 7장
- 원고별 이미지 기획 8개
- 네이버 본문 3,200~3,500자
- 티스토리 본문 2,501~3,000자
- 제목 중복 없음 (오늘 내부 + 과거 이력 대조)
- 네이버 지정 인사말 포함
- 😊 · 💙 · 😎 각각 1회
- MASTER FACT 존재 및 출처 1개 이상

하나라도 실패하면 배포하지 않는다.
