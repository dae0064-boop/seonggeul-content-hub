#  PR 검사
#
#  머지하기 전에 배관이 깨지지 않았는지 확인한다. 진짜 스레드나 네이버에 붙지 않는다.
#
#  원고 검사는 이 PR 에서 바뀐 .md 만 본다. 저장소에 남아 있는 옛 초안까지 걸면
#  손대지도 않은 글 때문에 빨개진다.

name: 검사

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: 문법
        run: |
          fail=0
          while IFS= read -r f; do
            node --check "$f" || { echo "문법 오류: $f"; fail=1; }
          done < <(find scripts -name '*.mjs')
          exit $fail
        shell: bash

      - name: 워크플로 YAML
        run: |
          python3 - <<'PY'
          import glob, sys, yaml
          bad = 0
          for f in sorted(glob.glob('.github/workflows/*.yml')):
              try:
                  d = yaml.safe_load(open(f, encoding='utf-8'))
                  assert d.get('jobs'), 'jobs 가 없습니다'
                  print(f'  OK  {f}')
              except Exception as e:
                  print(f'  실패 {f} — {e}')
                  bad = 1
          sys.exit(bad)
          PY

      - name: 스레드 자동화 자체 점검
        run: npm run threads:selftest

      - name: 바뀐 원고 검사
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          if [ -z "$BASE_SHA" ]; then
            echo "PR 이 아니라 비교 대상이 없습니다. 건너뜁니다."
            exit 0
          fi
          mapfile -t changed < <(git diff --name-only --diff-filter=d "$BASE_SHA" HEAD -- 'content/posts/*.md')
          if [ ${#changed[@]} -eq 0 ]; then
            echo "바뀐 원고가 없습니다."
            exit 0
          fi
          printf '검사할 원고: %s\n' "${changed[@]}"
          node scripts/lint-post.mjs "${changed[@]}"
        shell: bash
