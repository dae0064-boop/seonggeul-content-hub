# CLAUDE.md

성글벙글 콘텐츠의 **단일 저장소**. 글쓰기 규칙, 원고, 발행 자동화, 여러 대의 컴퓨터를
잇는 워크스페이스 도구가 모두 여기 있다.

이 문서는 **길잡이다.** 항상 필요한 것만 두고, 종류별 상세 규칙은 아래 표대로 따라간다.
규칙을 여기에 다시 적지 않는다 — 같은 규칙이 두 곳에 있으면 서로 어긋난다.

## 규칙을 어디서 읽는가

| 무엇을 하나 | 어디를 읽나 |
|---|---|
| 생활정보 원고를 쓰거나 고친다 | `.claude/skills/write-post/` — 주제 선정, 분량·키워드, 모바일 포맷, 고정 인사말, 구조, 서식, front matter |
| 발행 자동화를 손본다 | `.claude/skills/publish-naver/` — CDP attach, 선택자, 덤프, dry-run, 검증 범위 |
| 브랜드 말투·금지표현·출처를 확인한다 | `memory/CLAUDE.md` — 각 PC 의 `~/.claude/CLAUDE.md` 로 배포된다 |
| 보험·생활보장 글을 쓴다 | `standards/보험글-규칙.md` — **현재 중단 상태** |
| 여러 대의 컴퓨터에서 이어 작업한다 | `docs/WORKSPACE.md` |
| 새 컴퓨터에 설치한다 | `docs/SETUP.md` |
| 아직 정하지 못한 것을 확인한다 | `docs/OPEN-ISSUES.md` — 규칙을 손대기 전에 읽는다 |

스킬은 관련 작업을 시작할 때 자동으로 로드된다. 미리 열어보지 않는다.

**검사기(`scripts/lint-post.mjs`)가 세는 값이 최종이다.** 분량·키워드 횟수·줄 길이는
`write-post` 스킬과 검사기가 정본이고, 문서 설명과 다르면 검사기를 믿는다.

## 저장소 지도

```
CLAUDE.md          ← 이 문서. 길잡이
memory/CLAUDE.md   브랜드 기준 메모리. 각 PC 의 ~/.claude/CLAUDE.md 가 된다
.claude/skills/    작업별 규칙 (write-post, publish-naver, sns-research)
content/posts/     원고. <날짜>-<슬러그>.md (정본) + .json (스크립트 입력)
content/calendar/  발행 캘린더 아티팩트 소스, 키워드 목록
content/board/     원고 작업판 아티팩트 소스
scripts/           발행·검수 자동화(.mjs) + 워크스페이스 동기화 도구(.py)
launchers/         윈도우 더블클릭 실행기
standards/         운영 기준 문서
work/              2026-08 작업물 아카이브. 과거 기록이며 갱신하지 않는다
docs/              설치·워크스페이스·미결 사항
```

## 원고는 .md 와 .json 한 쌍

`.md` 가 정본이고, `.json` 은 거기서 생성한 스크립트 입력이다. `.json` 은 직접 고치지 않는다.

```bash
npm run check content/posts/<원고>.md    # .md → .json 변환 + 검사 (게이트)
```

검사를 통과하지 못하면 발행하지 않는다. `scripts/check_post.py` 는 지금 돌지 않는다 —
발행 여부는 `lint-post.mjs` 로만 판단한다.

**원고와 산출물은 이 저장소 `content/` 아래에 커밋한다.** 검사기와 발행 스크립트가
저장소 경로를 기준으로 동작하므로, 저장소 밖에 두면 검증이 돌지 않는다.
Google Drive 워크스페이스는 원고의 정본 위치가 아니다 (`docs/WORKSPACE.md`).

## 실행 환경에 대한 중요한 전제

이 저장소를 다루는 Claude 세션은 **Anthropic 클라우드 컨테이너**에서 돌 수 있다. 그 경우:

- 사용자 PC의 크롬에 접근할 수 없다. `localhost` 는 컨테이너 자신이다.
- `naver.com` 은 egress 정책에서 차단된다 (`connect_rejected`).

따라서 **네이버에 직접 붙는 작업은 클라우드 세션에서 수행할 수 없다.** 스크립트를 만들어
커밋하고, 실행은 사용자가 본인 PC에서 한다. 이 전제를 무시하고 "발행했습니다"라고
보고하지 말 것.

세션이 어디서 도는지는 `get_session` 의 `environment_kind` 로 확인한다.
`origin: desktop_app` 이어도 `environment_kind: anthropic_cloud` 면 클라우드다.

## 발행 캘린더

월별 발행 계획은 아티팩트로 관리한다. 소스는 `content/calendar/publish-calendar.html`.

**주소가 고정된 아티팩트다.** 매달 월말 내용만 갈아끼우고 주소는 유지한다.
다른 세션에서 갱신할 때는 반드시 `url` 파라미터를 넘겨야 한다 —
넘기지 않으면 새 아티팩트가 생겨 주소가 갈린다.
주소와 갱신 절차는 `content/calendar/ARTIFACT.md` 에 있다. 갱신 전에 반드시 읽는다.

캘린더의 `검증` 배지는 **날짜 근거를 실제로 확인한 항목에만** 붙인다.
계절성 추론으로 세운 글감에 붙이면 추측이 근거처럼 보인다.

## 브랜치

작업 브랜치는 `claude/*`. `main` 에 직접 푸시하지 않는다.

## 컨텍스트 아끼는 습관

매 턴 전체 대화가 다시 실려 나간다. 그래서 무엇을 읽었는지가 사용량을 정한다.

- **원고 한 편 = 한 세션.** 끝나면 `/clear`. 캘린더 갱신·스크립트 수정은 별 세션으로.
- **`work/` 아카이브와 카드뉴스 PNG 는 읽지 않는다.** 과거 기록이고 갱신하지 않는다.
- **아티팩트 HTML(`content/board/`, `content/calendar/`)은 부분 편집한다.**
  통째로 다시 쓰면 20~26KB 가 읽기·쓰기 양쪽에 실린다.
- **원고를 고칠 때 전문을 다시 읽지 않는다.** 검사기 출력의 줄 번호로 그 자리만 고친다.
- 파일은 필요한 범위만 본다 (`sed -n '40,80p'`). 통째 읽기는 마지막 수단.
- 한 시간 넘게 쉬었으면 이어가지 말고 새 세션에서 시작한다 (프롬프트 캐시가 만료된다).

# Compact instructions

대화를 압축할 때는 **지금 쓰고 있는 원고의 현재 상태와 검사기가 남긴 미해결 항목**,
그리고 사용자가 고른 결정(주제·제목·타겟)을 남긴다.
이미 통과한 검사 결과, 읽었던 파일의 전문, 탐색 과정은 버린다.
