# 성글벙글 콘텐츠 허브

집·회사·노트북 어느 컴퓨터에서 작업해도 같은 결과물을 얻기 위한 저장소다.
**모든 작업 결과물과 메모리는 구글 드라이브에 저장·백업한다.**

## 저장 위치 규칙

작업 결과물은 반드시 아래 디렉터리 중 하나에 만든다. 저장소 루트나 임시
디렉터리에 흩어 두지 않는다. 각 디렉터리는 드라이브 폴더
`내 드라이브/성글벙글 원소스 멀티콘텐츠 자동화 구현/` 아래로 그대로 동기화된다.

| 저장소 디렉터리 | 드라이브 폴더 | 용도 |
|---|---|---|
| `standards/` | `00_기준문서/` | 운영기준, SEO 지침 등 오래 가는 규칙 문서 |
| `work/` | `01_작업/` | 날짜별 콘텐츠 작업물 (`work/YYYY-MM-DD_주제/`) |
| `schedule/` | `02_발행일정표/` | 발행 일정표 |
| `memory/` | `09_메모리/` | 작업 중 축적되는 메모리 |
| — | `_archive/` | 2026-08-21 재구성 이전의 옛 자료 |

## 메모리 규칙

작업하며 알게 된 것 — 결정, 기준, 반복되는 패턴, 사용자 선호 — 은 대화 안에만
두지 말고 파일로 남긴다. 그래야 다른 컴퓨터에서 이어서 작업할 수 있다.

- `memory/decisions.md` — 확정된 결정과 그 이유. 새 항목은 맨 위에 날짜와 함께.
- `memory/preferences.md` — 사용자의 작업 방식 선호.
- `memory/sessions/YYYY-MM-DD.md` — 그날 무엇을 했고 다음에 무엇을 이어가야 하는지.

세션을 마무리할 때 그날의 세션 노트를 남기고, 새로 정해진 것이 있으면
`decisions.md`에 반영한다. 이 `CLAUDE.md` 자체도 메모리로 취급되어
드라이브 `09_메모리/CLAUDE.md`로 백업된다.

## 동기화 동작 방식

두 경로로 이중화되어 있다. 하나가 안 되는 환경에서도 다른 하나가 동작한다.

1. **구글 드라이브 데스크톱** (집·회사·노트북) — 세션 시작 시 드라이브에서
   가져오고(`pull`), 세션이 끝날 때 드라이브로 내보낸다(`push`).
   `.claude/settings.json`의 SessionStart / Stop / SessionEnd 훅이 자동 실행한다.
2. **git** — 어느 컴퓨터에서든 커밋·푸시하면 정본이 남는다.

수동으로 실행할 때:

```bash
./scripts/drive-sync.sh status   # 연결 상태 확인
./scripts/drive-sync.sh pull     # 드라이브 -> 저장소
./scripts/drive-sync.sh push     # 저장소 -> 드라이브
```

동기화는 **양쪽 모두 파일을 지우지 않는다.** 더 새로운 파일이 오래된 파일을
덮어쓸 뿐이다. 그래서 한쪽에서 지운 파일이 다른 쪽에서 되살아날 수 있다 —
정말 지울 때는 양쪽에서 지우거나 `_archive/`로 옮긴다.

드라이브 데스크톱 앱이 없는 환경(원격 컨테이너 등)에서는 훅이 조용히 넘어가고
git과 구글 드라이브 커넥터로만 작업한다. 그 경우 결과물은 커밋해서 남긴다.

## 작업할 때

- 새 콘텐츠 작업은 `work/YYYY-MM-DD_주제/` 폴더를 만들어 그 안에서 한다.
- 작업이 끝나면 커밋한다. 커밋하지 않은 것은 다른 컴퓨터에서 볼 수 없다.
- 사용자는 한국어로 소통한다. 답변과 문서 모두 한국어로 쓴다.
