# 2026-08-21

## 한 일

여러 컴퓨터(집·회사·노트북)에서 같은 상태로 이어 작업하기 위한 기반을 만들었다.

- 드라이브 기준 폴더를 `내 드라이브/성글벙글 원소스 멀티콘텐츠 자동화 구현/`으로 정하고
  `00_기준문서 / 01_작업 / 02_발행일정표 / 09_메모리 / _archive` 구조로 재편했다.
- 재편 이전 자료 3건은 `_archive/`로 옮겼다 (삭제하지 않음).
- `scripts/drive-sync.sh` — 드라이브 데스크톱 폴더를 자동으로 찾아 저장소와
  양방향으로 복사한다. Windows / macOS / WSL 경로를 모두 탐색하고,
  드라이브가 없는 환경에서는 조용히 넘어간다.
- `.claude/settings.json` — SessionStart에서 pull, Stop / SessionEnd에서 push 하는 훅.
- `CLAUDE.md`, `memory/`, `docs/SETUP.md` 작성.

## 다음에 할 일

- 컴퓨터 세 대에서 각각 `./scripts/drive-sync.sh status`를 한 번씩 실행해
  드라이브 폴더가 제대로 잡히는지 확인한다. 안 잡히면 `.drive-local`에 경로를 적는다.
- `_archive/`에 넣어 둔 운영기준 문서와 발행일정표 중 계속 쓸 것을
  `standards/`, `schedule/`로 꺼내 온다.

## 오후 · SNS 자동화 에이전트 (서칭 단계)

전체 그림: 리서치 → 주제 3개 → 주제당 제목 3개 → 사용자가 선택 → 포스팅 글 → 카드뉴스.
이번엔 **서칭 단계까지만** 만들었다.

- 이 웹 세션(claude.ai/code)의 이그레스 정책에서 `openapi.naver.com`,
  `api.apify.com`, `graph.facebook.com`이 막혀 있다. `googleapis.com`만 열려 있다.
  → 네 소스를 다 쓰려면 **사용자 PC에서 실행해야 한다.** 사용자는 윈도우 설치를 선택.
- 인스타는 에피파이(Apify) 경유, 네이버는 오픈API 키로 가기로 정함.
- `scripts/research.mjs` — 외부 패키지 없이 동작. `--check`로 키 자가 진단.
- `.claude/skills/sns-research` — 원자료 + WebSearch를 합쳐 제목 9개 제안.
- `docs/윈도우-설치.md` — 비개발자 기준으로 작성. 드라이브 `00_기준문서/`에도 올림.

## 다음에 할 일

- 사용자가 윈도우 PC에서 1~8단계 설치. 단계마다 확인해주기.
- `--check`가 전부 ✅ 나오면 첫 리서치 돌려보기.
- 그 다음 포스팅 글 작성 단계, 카드뉴스 단계를 이어서 만든다.
  (카드뉴스는 Chromium + Playwright로 HTML→PNG, 한글 폰트는 Google Fonts Noto Sans KR)

## 사용자 관련 메모

- 스스로 "컴맹"이라고 함. 명령어는 **복사해서 붙여넣을 수 있는 형태로** 한 줄씩 주고,
  단계마다 "여기까지 됐는지" 확인받는다. 전문용어는 풀어서 쓴다.
- API 키를 채팅창에 붙여넣지 않도록 계속 주지시킬 것.

## 저녁 · 클라우드 자동화 가동

- 사용자가 **클라우드 실행**을 선택했다. PC를 안 켜도 되는 대신 네이버 API와
  인스타(Apify)는 못 쓴다. WebSearch 기반으로 간다.
- **매일 아침 트리거 생성** — `trig_01BnmiYRoBXnrtW6hvdtM7Zu`
  cron `33 23 * * *` (UTC) = 한국시간 08:33. 매 실행마다 새 세션이 뜬다.
  푸시 알림 켜둠.
- **매일 갱신되는 링크**: https://claude.ai/code/artifact/5d56f2e8-bd09-42f8-b7a8-92fa8b700a17
  트리거 프롬프트에 이 주소를 박아두었다. `url` 파라미터로 덮어써야 주소가 유지된다.
- **주의**: 트리거가 띄우는 세션에는 **MCP 커넥터가 안 붙는다.** 구글 드라이브
  업로드가 불가능해서 보관은 git push만으로 한다. 사용자가 자기 PC에서 저장소를
  열 때 drive-sync 훅이 드라이브로 옮겨준다.
- 첫 회 원고를 냈다. 1주차 권고에 맞춰 채널당 대표 1편씩만 완성본, 나머지 4편은
  개요. 사용자가 요청하면 6편 다 채운다.

## 다음에 확인할 것

- **내일 아침(8/22) 08:33 첫 자동 실행이 제대로 도는지.** 특히 Artifact 도구를
  쓸 수 있는지 — 못 쓰면 링크가 갱신 안 된다. 안 되면 다른 전달 경로를 찾아야 한다.
- 사용자가 네이버 API 키 발급에서 막혀 있다. 급하지 않으니 나중에.

## 밤 · 모바일 가독성 전환, 6편 완성

- 사용자 요청으로 **6편 전부 완성 원고**로 채웠다. 네이버 3 + 티스토리 3.
- 모바일 기준을 조사해 적용했다. 네이버 20자 강제 개행, 티스토리는 개행 없이
  문단만 짧게. `standards/발행-운영기준.md`에 반영.
- `scripts/brief-render.mjs` 신규 — 원고 폴더 → 링크 페이지. HTML 구조 검증까지
  돌려 태그 불일치 없음을 확인했다.
